# Releases

Use this folder for frozen release snapshots.

Suggested structure:

- `releases/2026-05-20/`
- `releases/v1.0.0/`

Put only the files that belong to a published version in each release folder.
